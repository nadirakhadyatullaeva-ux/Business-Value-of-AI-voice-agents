# Reproducibility snapshot for Chapter 5 quantitative analyses
# Tested with Python 3.12 (Linux x86_64). Pinned to known-working minimum
# versions; a `pip install -r requirements.txt` should reproduce all
# in-chapter figures and statistics from the inputs in 06_Data/.

# Core scientific stack
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0

# Visualization
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14

# NLP — sentiment analysis (lexicon-based)
vaderSentiment>=3.3.2

# NLP — topic modeling (BERTopic stack)
bertopic>=0.16,<0.18
sentence-transformers>=2.7,<5.0
umap-learn>=0.5.6,<0.6
hdbscan>=0.8.33,<0.9
scikit-learn>=1.4,<2.0

# Notebook execution
jupyter>=1.0
ipykernel>=6.29

# Document handling (financial metrics workflow)
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
